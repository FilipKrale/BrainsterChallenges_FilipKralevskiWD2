for(let i=10; i<=100 ; i++)
{
    if(i % 3 == 0 && i % 2 == 0)
    {
        console.log(`Number ${i} is even and divisble by 3.`)
        document.write(`Number ${i} is even and divisible by 3. <br>`)
    }
}